/**
 * @module task/components/TaskCard/types
 * @summary Type definitions for TaskCard component
 */

import type { Task } from '../../types';

/**
 * @type TaskCardProps
 * @summary Props for TaskCard component
 */
export type TaskCardProps = {
  task: Task;
};
